---
interface Props {
  eyebrow: string
  fullWidth?: boolean
}

const {
  eyebrow,
  fullWidth = true
} = Astro.props
---
<section class={fullWidth ? '' : 'container'}>
  <p class='eyebrow'>{eyebrow}</p>
  <h2><slot /></h2>
</section>

<style>
  .container {
    margin: 0 var(--container-margin);
  }
</style>
