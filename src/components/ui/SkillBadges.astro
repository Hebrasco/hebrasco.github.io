---
import Badge from 'components/ui/Badge.astro'
import type { Skill } from 'types'

interface Props {
  skills: readonly Skill[]
}

const { skills } = Astro.props
---
<div class="skill-tags">
  {skills.map((({ name}) => <Badge label={name} />))}
</div>

<style>
  .skill-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.25rem;
  }
</style>
